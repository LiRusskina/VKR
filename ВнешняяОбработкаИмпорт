&НаКлиенте
Процедура ПутьКФайлуНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	ДиалогОткрытияФайла = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.Открытие);
	ДиалогОткрытияФайла.Показать(Новый ОписаниеОповещения("ОбработкаВыбораФайлов", ЭтаФорма));
КонецПроцедуры         

&НаКлиенте
Процедура ОбработкаВыбораФайлов(СписокФайлов, ДополнительныеПараметры) Экспорт
	ПутьКФайлу = СписокФайлов[0];
КонецПроцедуры 

&НаКлиенте
Процедура Загрузить(Команда)
	ЗагрузитьНаСервере();
КонецПроцедуры

&НаСервере
Процедура ЗагрузитьНаСервере()
	Excel = Новый COMОбъект("Excel.Application");
	Workbook = Excel.Workbooks.Open(ПутьКФайлу);
	Sheet = Workbook.Worksheets("Sheet1");
	НомерСтроки = 2;
	Пока ЗначениеЗаполнено(Sheet.Cells(НомерСтроки, 1).Value) Цикл
		НоваяСтрокаУченики = Ученики.Добавить();
		НоваяСтрокаУченики.ФИО = Sheet.Cells(НомерСтроки, 2).Value;
		НоваяСтрокаУченики.ДатаРождения = Sheet.Cells(НомерСтроки, 3).Value;
		НоваяСтрокаУченики.Адрес = Sheet.Cells(НомерСтроки, 4).Value;  
		НоваяСтрокаУченики.КонтактныйТелефон = Sheet.Cells(НомерСтроки, 5).Value;
		НомерСтроки = НомерСтроки + 1;
	КонецЦикла;
КонецПроцедуры
